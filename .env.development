# Local development must use the local API so server-side lock changes can be tested.
# Production continues to use .env.production / the deployed Render API.
VITE_API_BASE_URL=http://localhost:5000/api
